import { SortableData } from './sortablejs.directive';
/** @internal */
export declare class SortablejsBinding<T> {
    private readonly target;
    constructor(target: SortableData<T>);
    insert(index: number, item: any): void;
    get(index: number): T;
    remove(index: number): T | (0 extends 1 & T ? import("@angular/forms").AbstractControl<any, any> : T) | undefined;
}
