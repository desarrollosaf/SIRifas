import { SortablejsBinding } from './sortablejs-binding';
import { SortableData } from './sortablejs.directive';
/** @internal */
export declare class SortablejsBindings<T> {
    bindings: Array<SortablejsBinding<T>>;
    constructor(bindingTargets: Array<SortableData<T>>);
    injectIntoEvery(index: number, items: any[]): void;
    getFromEvery(index: number): T[];
    extractFromEvery(index: number): (T | (0 extends 1 & T ? import("@angular/forms").AbstractControl<any, any> : T) | undefined)[];
    get provided(): boolean;
}
